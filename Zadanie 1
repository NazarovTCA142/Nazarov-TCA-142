#include <iostream>
#include <cmath>
using namespace std;
/**
  *@brief функциянахождения a
*@param x - аргумент x
  *@param y - аргумент y  
  *@param z - аргумент z 
*@returnвозвращаетзначениеa
*/
double Geta (const double x, const double y, const double z);
/**
*@brief функция нахождения b
*@param x - аргумент x
*@param y - аргумент y  
*@param z - аргумент z 
*@return возвращаетзначение b
*/
double Getb (const double x, const double y, const double z);
/**
  *@brief главная функция программы
  *@return возвращает 0 в случае корректного выполнения, иначе 1
*/
int main() 
{ 
  const double x = 0.3; 
  const double y = 2.9; 
  const double z=0.5; 
cout<< "x = "<< x <<"  y = "<< y <<"  z = "<< z <<endl;
cout<< "a = "<<Geta(x, y, z) <<"  b = "<<Getb(x, y, z) <<endl;
  return 0;
}

double Geta(const double x, const double y, const double z)

{
    return (z * z * x) + exp(-x) * cos(y * x) / (y * x - exp(-x) * sin(y * x) + 1);
}

double Getb(const double x, const double y, const double z)
  {
    return);  
}
